/**
 * Single source of truth for site identity.
 * Everything a new owner needs to change lives here.
 */
export const site = {
  url: 'https://github-markdown-blog.pages.dev',
  title: 'tanghoong',
  handle: '@tanghoong',
  bio: 'Writing in markdown, publishing with git push.',
  // Shown in the avatar when no image is set. First character is used.
  avatar: null,
  github: 'tanghoong/github-markdown-blog',
  // Locale used for date formatting.
  locale: 'en',
}
